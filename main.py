# main.py

import mean_var_std
from unittest import main

# Imprimir el resultado de la función calculate con una lista de ejemplo
print(mean_var_std.calculate([0, 1, 2, 3, 4, 5, 6, 7, 8]))

# Ejecutar las pruebas unitarias automáticamente
main(module='test_module', exit=False)
